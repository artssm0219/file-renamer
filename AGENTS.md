# Project Instructions

このプロジェクトは、AIを使った小規模開発の練習用です。

## 方針
- まずREADMEに仕様を整理してから実装する
- ファイル操作を扱うため、安全性を最優先する
- 最初は dry-run を基本にする
- 実際にファイル名を変更する処理は、明示的な --apply がある場合のみ行う
- 既存ファイルを上書きしない
- エラー時は初心者にも分かる日本語メッセージを出す
- 実装後はpytestで確認する
- APIキーや個人情報は扱わない

## 技術方針
- Pythonで実装する
- 標準ライブラリを優先する
- 外部サービスや有料APIは使わない